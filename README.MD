# 🎮 GameMode Switcher

Быстрое переключение ПК в игровой режим: Steam Big Picture + ТВ как основной экран.  
При выходе из Big Picture — автоматический возврат на основной монитор.

## Что делает

1. Закрывает Steam (если запущен)
2. Запускает Steam в режиме Big Picture
3. Переключает вывод изображения на ТВ (`DisplaySwitch /external`)
4. Запускает фоновый наблюдатель — как только Big Picture закрывается, возвращает экран обратно (`/internal`)

## Требования

- Windows 10/11
- Steam установлен в `C:\Program Files (x86)\Steam\`
- Конфигурация: **1 монитор + 1 ТВ** (HDMI/DP)

## Использование

Запустить `game_mode.bat` от имени обычного пользователя.  
Для удобства — создать ярлык на рабочем столе или повесить на горячую клавишу (например, через AutoHotkey).

## Ограничения

| Сценарий | Статус |
|---|---|
| 2 экрана (монитор + ТВ) | ✅ Работает |
| 3+ экрана | ⚠️ Не поддерживается (`DisplaySwitch` не адресует конкретный экран) |
| Переключение звука на ТВ | ❌ Не реализовано (требует NirCmd или AudioDeviceCmdlets) |
| Ноутбук + ТВ | ✅ Работает надёжно |

## Кастомизация

Путь к Steam меняется в строке:
```bat
set STEAM="C:\Program Files (x86)\Steam\Steam.exe"
```

Таймаут ожидания запуска Big Picture (по умолчанию 120 × 0.5с = 60 сек):
```powershell
$timeout = 120
```
